<div  class="form-group">

<%= bootstrap_form_for(vehiculo) do |f| %>
  <% if vehiculo.errors.any? %>
<div id="error_explanation">
      <h2><%= pluralize(vehiculo.errors.count, "error") %> prohibited this vehiculo from being saved:</h2>

      <ul>
      <% vehiculo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <!--<%= f.label :marca %>-->
    <%= f.text_field :marca %>
  </div>

  <div class="field">
    <%= f.text_field :modelo %>
  </div>

  <div class="field">
    <%= f.text_field :color %>
  </div>

  <div class="field">

    <!--<%= f.text_field :tipo %>-->
    <!--ComboBox-->
    <%= f.select :tipo, options_for_select([["Automovil", "Automovil"], ["Compacto", "Compacto"], ["Camioneta", "Camioneta"], ["Camión", "Camion"]]) %>

  </div>

  <div class="field">
    <%= f.text_field :kilometraje %>
  </div>

  <div class="field">
    <%= f.number_field :capacidadPasajeros, in:1..50,label: "Capacidad de pasajeros", placeholder: "Max. 50" %>
  </div>

  <div class="field">
    <%= f.text_area :observaciones %>
  </div>

    <div class="field">
      <%= f.file_field :image, label:"Imagen" %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>